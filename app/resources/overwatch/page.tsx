import OverwatchLookup from "@/components/OverwatchLookup";

export const metadata = { title: "Overwatch" };

export default function Page() {
  return (
    <main style={{padding:24}}>
      <OverwatchLookup />
    </main>
  );
}
